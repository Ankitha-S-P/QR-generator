# QR code image generator

- Clone the repository/dowload zip file and extract. Open the project folder in VScode or other javascript supported IDE.
- run 'npm install' in the project folder terminal
- run 'node index.js' in the project folder terminal

You'll find the qr code image in the project folder
